# NSE Stock Scanner — HTML Migration

Run API:
```bash
cd backend
pip install -r requirements.txt
uvicorn api:app --reload
```

Open `frontend/index.html` with a static web server. The project expects JSON data folders at repository root with the original names.
